{
  "name": "node-physics-demo",
  "type": "module",
  "scripts": {
    "build": "tsc && mkdir -p dist/proto && cp proto/game.proto dist/proto/",
    "start": "node dist/main.js"
  },
  "dependencies": {
    "protobufjs": "^7.2.5"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}